# 实施过程中采用的裁决

日期：2026-09-20。按发生顺序保留全部已记录裁决，包括转向dsh之前的历史选择；后续以用户指定dsh优先的范围覆盖旧宿主顺序。每项包含原因与代价，原始本地ledger保留，不删除证据。

1. 沿用已展示的 /amoji → amoji_pick → 配套面板流程继续验收，作为本轮实现选择 — 用户要求继续已展示的方案，入口可逆，日常三工具合同不变 — 若用户需要无需先调用 /amoji 的主动入口，需要另做宿主输入适配。

2. 新增独立工作树进行后续改动 — 本次继续开发授权涵盖可逆的代码隔离，工作区原来干净 — 代价是本地验收通过后需要整合分支；不因此请求重复许可。

3. 01通过允许进入02 — 原五条验收及独立复核均有具体证据，配套面板是批准的视觉路径 — 不放宽后续CRUD、投递状态或其他宿主实现要求。

4. 02针对共享服务的新增真实收发采用已安装插件的Codex CLI 0.153.4及配套面板，两个独立会话并发连接 — 02验收要求独立Codex会话，01已通过真实Desktop完整接入；此轮避免再改Desktop临时provider配置，最终19汇总Desktop完整基准 — 本票新增证据明确称CLI，不声称已单独实测Desktop共享核心。

5. 02门槛按实际交付行为关闭，保留测试驱动收尾与后续502失败 — 两真实任务均完成收发/最终回应，四条对应消息rendered；重启及去重有真实进程合同证据，独立复核通过 — 不据此声明新版Desktop或所有端到端脚本通过，19仍须完整验收。

6. 05以后的功能先逐票完成实现、必要公开合同检查与审查，真实Codex产品旅程在19集中执行并将同一份实际证据回填相关票据 — 用户质疑长时间重复运行，01已证明接入路线、02已证明共享核心；后续不需要对每个小功能重复整个模型矩阵 — 实际模型/面板验收项仍保留未完成，不降低20个最终场景要求，不把集中验收前的实现完成称为整票或整期交付。

7. 2026-09-19 用户明确改为 dsh 主适配与验收，Codex/Claude 新增适配暂缓 — docs/planning/dsh-focus-2026-09-19.md 覆盖旧宿主顺序，复用全部已完成共用核心 — 若共用接口改变仍需必要本地兼容检查，不因此声称旧端已验。

8. D1真实界面缺陷先于依赖它的生命周期工作解决 — 协议调用成功不能证明人和AI自然表达，这是产品核心目的 — 代价是一次聚焦修补/复审和一次有预算的首次发送复验。

9. 诊断Flash网关续写的同时推进D2 — 共用版本、来源和CAS不依赖供应商思考传输；最终仍要求无错误工具回合 — 代价是暂保留该网关失败作为未关闭接入项。

10. 仅在QA使用Flash的Chat兼容配置 — 已安装适配器明确支持DeepSeek格式，真实Responses两次出现不同传输失败，而Chat完整回合成功 — 代价是支持结论只覆盖已验证配置，不声称用户原Responses配置通过。

11. D3按协议5.1执行：导入已有条目的新版本不自动切换默认；上游默认更新仅在人明确CAS选择时生效 — 保护用户固定选择并消除交接歧义 — 代价是D4需要显式selectRevision管理入口。

12. 最终dsh独立QA将输出上限从768降低为256，同一Flash/Off/零重试；为仍需真实执行的30对场景及收尾旅程，初始总guard32/2MiB改为累计160requests/8MiB，逐case allowance仍冻结16，未发新请求。理由是覆盖批准的60个独立A/B会话而保留明确预算，成本为最多144个额外短请求且第三方金额未知，遇异常不自动扩额/重试。配置只QA且下次Host重启生效，用户模型配置不变。

13. final fix同时增加一般性发送者主体/对象语义核对提示 — 正式thanks-1文字正确却选反向感谢语义，完整meta存在，问题在选择指引；不新增协议字段或强制使用 — 成本是后续可能更克制，保留原评测并仅定点复验，不以新例抹掉原失败。

14. 感谢案例最终定点复验首次遇网关500；在随后独立limit请求成功证明网关恢复后，仅补一次上限3请求的人工复验 — 首次没有模型答案无法评估语义修补，新增证据以独立文件保留且绝不覆盖正式30对 — 成本最多3次Flash短请求，若仍失败停止付费尝试，不自动扩额。


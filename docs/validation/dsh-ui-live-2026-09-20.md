# dsh 原生界面实测：2026-09-20

这是 D4 的局部真实浏览器证据，不是完整发行验收。安装源码为 `9d37e6e`，宿主 `@deepseek-ai/dsh@0.1.5-rc.2`，Node 24.19.0，macOS arm64；使用独立 `amoji-qa` profile、专用工作区及数据目录。仅本地页面和管理操作，本轮新增模型请求为 0。已知五分钟连接失效尚待 D5 修复，D4 审查发现的四项边界问题也仍待修复。

## 实际安装与历史显示

通过宿主官方 `plugin add` 安装本次独立路径 tarball，使用 `CI=true` 与 `--ignore-scripts`，命令退出 0。重启的是专用 QA Host；核心由已安装插件的 `node_modules/@amoji/dsh/runtime/src/service-main.js` 启动，没有先用工作树客户端启动替代核心。

打开先前已经完整完成的 Flash 会话后，工具组默认折叠时，鼓励动图直接显示在普通对话中。展开工具组后，工具结果显示文字状态，主流保留一张图片，没有重复。精确消息为 `e58dacd2-3c97-48cf-a006-541be5d4aa3e`；这次使用的是历史恢复，没有重新请求模型。

选择器显示原有 3 项和新基础库 24 项，使用固定三列网格。管理按钮在同一页面打开表情库、创作与草稿、AI 偏好、导入与导出四个区域。

## 实际创作和完整包

用浏览器文件选择器上传受控 `thanks.png`，输入感谢陪伴的意图。界面明确说明建议由本机文字规则生成，没有读取图片或调用模型。在独立候选区域修改名称和固定含义后明确采用，保存并预览；图片加载后勾选核对，再确认加入共享库。

新表情 `QA 一起弄明白` 随即显示在共享库，可查看固定义与版本：

- asset：`facf5bd8-ac43-47ca-a3ca-a8ad78cb8f22`
- revision：`0e63ec92-5310-4ab6-a844-932b847b8705`

选择该版本导出，浏览器实际下载 `我的表情.amoji`，913460 字节，包含 `manifest.json` 和一个按摘要命名的 blob。清单只含包信息、所选固定版本与默认引用，没有会话、个人设置或凭据。自动化的 download 事件等待超时，但随后实际文件与界面成功状态均已核对；不能将事件钩子的超时描述成导出失败。

再用浏览器文件选择器导入刚下载的文件，界面返回“新增 0 个版本，已有 1 个”，没有重复创建。

## 未关闭项

- 预览图片迟到、条目异步切换、包导入未知结果、并发编辑保留上传素材的四个审查问题需修复后限定复验。
- 草稿列表按钮被拉伸过大、保存后列表名称未及时刷新，已转交修补。
- 偏好、归档、副本、版本选择、多会话、可靠重连和完整安装生命周期的最终真实验收仍待执行。
- 本轮没有将 UI 成功推断为长期稳定，也未将历史图片恢复算作新的模型语义或成本测试。

本地原始摘要为 `.local/dsh-focus/D4/live-checks.json` 与 `browser-export.json`；日志含专用实例认证材料，不随公开证据分发。

## ea0fc1e 修复版追加实测

四项审查问题已修复并通过限定复审。再次经官方 CLI 安装修复版，退出 0；原有 28 项数据保留。真实页面确认草稿列表恢复紧凑尺寸，保存后的名称立即更新。

内置“好开心”经原生界面创建“QA 分享开心”个人副本，保存、预览和明确确认后成为新的 local asset，derived_from 指向原内置固定版本，原内置项保持不变。共享偏好经 UI 保存为 warm/moderate/paused=true，独立的认证 Host RPC 读回一致，再经 UI 恢复 neutral/restrained/paused=false。个人副本归档后从可用列表移除，在归档视图可见，再恢复到可用列表。

浏览器实际下载的单项包另外经已安装插件 runtime 在全新目录恢复：初始 24 项基础库，导入后 25 项；恢复的完整 Expression 与原清单逐字段相同，素材 SHA-256 一致。临时核心在验收后经其精确服务身份正常停止，数据保留。

尝试继续编辑已确认草稿时，原生 JavaScript 确认框阻塞浏览器自动化；关闭接口未取得弹窗句柄，键盘和标签关闭也超时。该处剩余编辑旅程未据此声称通过，已请求人工关闭测试弹窗，连接可靠性实施继续进行。本次追加仍为 0 个模型请求。

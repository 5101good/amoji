# 双画风与点选发送验收

日期：2026-09-21。代码分支：`codex/expression-themes`，实现与测试提交截至 `dec32bf`。

## 交付行为

- dsh 聊天框表情入口为 28px，与原生指令、附件按钮实测同高。
- 点表情直接发送；详情按钮仅查看固定语义。快速双击使用同步锁，不确定结果保留原请求身份供核对。
- 默认经典画风，延续原三款奶白角色；新增办公画风，以简洁灰蓝纸张角色表达相同语义。每套 16 个，无重复语义条目，切换后持久化。
- 画风与 AI 语气偏好独立，AI 候选也使用当前画风。已有消息使用原精确版本，切换不替换历史图片。
- 新素材共 29 次独立 imagegen 生成，经典另复用原三款。31 个静态主图为 512×512；经典鼓励动画保留原 887×887 主文件和 poster。UI 显示统一为 76px 缩略图、152px 消息图。

## 自动验证与审查

- `npm test`：109/109 通过。
- dsh 完整测试：82/82 通过。
- 素材专项：5/5 通过，包含两画风同名同语义、原 14 个语义不变、原鼓励动画字节保留。
- TypeScript 检查、真实 dsh 浏览器包构建、`git diff --check` 通过。
- 独立初审的三项 Important 已修复并复审；最终分支审查 Critical 0、Important 0。

## 本地升级与数据

升级前保存日常 profile 文件、SQLite 一致备份及 blobs。先在副本迁移：默认 17 个条目整理为经典 16 个不重名条目，旧 41 个 revision 和 4 份 session 内容保持。只归档精确旧默认引用，不删除历史或用户素材。

官方 CLI 安装到日常 `web` profile，确认原 host 空闲后激活新版。共享服务切换前关闭本任务自身持有的旧 MCP lease，通过公开 stop 接口停止旧服务。日常 3080 已恢复，新服务正常。

安装后对比 `client.js`、`base.amoji`、`library-store.js`、`expression-appearance.js` 与本次构建字节完全一致；日常数据库中的原 41 个 revision 再次逐一比对保持原内容。

安装包：`amoji-dsh-0.1.0-dev.1.tgz`，5,304,263 bytes。

SHA256：`9376e0eaff0889f2ea87735ff7af2df783db1f59944655442deefe788f213e66`。

## 实际 Edge 验收

使用用户日常 `http://127.0.0.1:3080/` 页面，浏览器扩展控制；独立 QA 地址被客户端拦截，没有绕过该拦截。

1. 原 `hi` 会话历史仍显示旧“击个掌”“一起庆祝”“这个不错”的原图。
2. DOM 实测：指令 28px、附件 28px、表情 28px；表情字体 13px。
3. 经典、办公各显示 16 个对应语义。实际截图确认角色画风不同，经典恢复奶白角色，办公使用灰蓝线条纸张角色。
4. 办公“谢谢”的详情仅展开语义；原会话仍为 6 轮，没有发送或触发模型。
5. 刷新页面再打开选择器，办公选项仍选中，偏好保存成功。
6. 新建专用验证会话，确认模型为 `deepseek-v4-flash`，单击办公“谢谢”。面板关闭，表情立即进入新会话，未点击额外发送按钮。
7. 模型回复“不客气，能帮上忙就好 😊 有需要随时说。”；UI 显示 1 轮、1 步、3 秒、10.8K 总 token、10% 缓存命中。只进行这一次模型验证轮次，没有使用 gpt-reserve；此 token 数包含宿主上下文，不等于新增文字长度，未据此推算账单金额。
8. 恢复经典偏好后，已发送的办公“谢谢”仍保持办公图片，消息图实测 152×152。

快速双击、CAS 冲突、投递结果未知等时序边界由自动化测试验证，本次真实页面没有人为制造网络故障。新验证会话保留以便用户检查。

本次工作已本地提交并安装；未合并 main、未推送远端。

# dsh 产品体验重构验收

日期：2026-09-21。范围为 `4f9d01d` 后的选择器、管理页面、消息展示和协作内容重构。其他端没有新增适配目标。

## 当前结论

代码与隔离 QA 主流程已验证，**尚不能声明所有功能均已完整验收或已发布**。日常 web profile 仍是此前版本，本轮只更新 amoji-qa。发布前仍需日常入口验收，以及重构后管理写操作完整真机复验；不以旧页面验收替代本次所有新页面路径。

## 本轮实现

- 选择器支持入口再次点击、关闭按钮、Escape、外部点击及焦点离开；发送被宿主接纳后收起。未知投递保留原 requestId，不自动另发。
- 图与名称使用同一个选择按钮；可信用户消息仅去除精确插件语义，保留其他文字、附件、引用标签和技能信息。语义/版本折叠查看。
- 表情库与创作导航重做，基础字段优先，高级字段渐进展开；桌面详情分栏、窄屏独立详情。
- 默认库改为 14 张同风格透明 PNG，按进度、澄清、方向、反馈分类；固定语义支持协作意图，不扩大授权、不虚报任务状态。
- 升级仅归档精确匹配的旧 builtin 默认项，保留旧 revision、素材、历史、个人副本和 imported 内容；重启不覆盖用户后来恢复/归档的选择。
- 小高度模式改为两栏选择/语义，中间分别滚动，关闭和发送固定可达。

## 自动验证

- `npm run build:dsh`：通过。
- `npm test`：105/105，通过。默认内容从 24 项变为 14 项，依赖旧计数的断言已更新；动图功能仍使用独立动画样本验证，没有删除动画覆盖。
- 最终 `tsx --test tests/dsh-*.test.ts`：77/77，通过。
- 协作内容/迁移 4 项包含于通用套件；引用标签保留和低高度结构分别观察到 RED 后修复 GREEN。
- `git diff --check`：通过。

## 实际安装与页面

使用官方 `dsh plugin --profile amoji-qa add ... --ignore-scripts` 安装 product-ui-5。安装目录的 client.js、基础包、迁移政策和库实现与构建目录字节相同。

CUA 实际观察：桌面深/浅主题，320×568 选择/展开语义，568×320 横屏两栏，分类、外部点击收起、发送后收起、管理窄屏详情/返回、旧表情历史与新表情历史。主题恢复为跟随系统，视口恢复默认。最终低高度修复已安装并再次截图确认。

最终 tgz：`.local/release-preparation/product-ui-5/amoji-dsh-0.1.0-dev.1.tgz`。
SHA-256：`165e13217a00fbbd419afddf2a6fb8abce6658b938502212e58242db84fc4210`。

## 真实模型与费用控制

本轮只新增 2 次 `deepseek-v4-flash`，每次最多 256 输出 tokens，推理 Off；观测请求 108/109 均为 0 image blocks、0 image data URLs。没有运行新的一整批评测。额度冻结为累计 109。

- 进度如何：模型如实说明当前会话没有在跑任务，没有编造进度或后台工作。
- 举个例子：模型给出具体示例，并明确示意不是已发生事实；回答因 256 tokens 限额截断，没有为补齐尾句再发请求。

这些是两个场景的实际证据，不是所有模型、全部表情响应质量保证。账单金额不可得。

## 独立审查与剩余项

一次 fresh reviewer 发现 1 个 Important：小高度裁切发送区，主代理在真实 568×320 页面复现，已修复并以实际安装包复验；未发现其他可证实重大问题。

1. **日常入口阻塞**：本轮再次访问 http://127.0.0.1:3080/，应用内浏览器仍返回 ERR_BLOCKED_BY_CLIENT。未绕过，也未把 QA 页面当作日常页面验收。日常 profile 本轮未更新。
2. **管理写操作待完整复验**：新建、上传、建议、预览确认、包导入导出、冲突和重连有自动检查及此前版本真机证据；本轮重构后的管理页面目前完成浏览、详情、返回、草稿表单检查，尚未把所有写操作重新走完。
3. **Minor 可访问性**：窄屏关闭详情时，焦点恢复在 React 提交前执行，可能落不到此前隐藏的表情按钮；不影响点击返回，需要后续完善。
4. 尚未合并到 main、推送或公开发布。工作保留在 codex/v0.1-completion。

QA Host 65501 保留运行供继续验收，用户的日常 Host 未停止。数据、旧历史和用户素材均未删除。

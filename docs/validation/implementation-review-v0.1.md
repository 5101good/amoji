# 票据 01 实现审查与修复

当前结论：2026-09-15 票据01已关闭；后文09-05条目是历史记录，最新两轴复核见末尾补充。

日期：2026-09-05。文档基线 `a30705d`；初次实现 `ab473c7`，随后对修复逐项复审。审查依照 mattpocock code-review 技能，由 Standards 与 Spec 两名独立审查代理进行，固定比较入口为 `git diff a30705d...HEAD`。范围是 Codex 三样本小样；不将后续票据空缺伪装为完成。

## Standards

累计四项 P2 均已修复并复审关闭：

1. 宿主关闭 stdin 后服务未退出：统一处理 stdin EOF、SIGTERM、SIGINT，并关闭面板、待选请求和 Journal。真实子进程生命周期测试通过。
2. 旧浏览器打开请求的延迟失败可能清除新选择：集中处理待选请求结束逻辑，只清除对应的 pending 实例。取消与延迟失败竞态测试通过。
3. 两个连接共用消息库时，一个运行时持续读取旧缓存：读取时对比持久版本并刷新，回执在同一快照上修改、保存。双连接读取和回执测试通过。
4. 新样本包可复用版本 ID 改变旧语义：事务登记已见版本，发布当前清单前比较定义；即使退出过清单，也拒绝同 ID 不同内容。`REVISION_CONFLICT` 测试通过。

审查另提醒了分发收尾事项，均已处理：生成的 `.mcp.json` 保留在本机但取消 Git 跟踪；README 明确每台机器重建启动配置及保留安装源目录的要求。没有将占位符配置失败包装为可移植支持。

本轴没有剩余已确认的代码缺陷。测试覆盖不等于不存在其他缺陷。

## Spec

原有代码问题的修复状态：

- 重复 `/api/select` 返回冲突：已完成请求优先返回原 `message_id`；同进程 HTTP 重试不重复发送。跨进程投递状态机仍属后续范围。
- 当前预览未展示完整定义：已补适用语境与完整版本/语义详情。复审发现历史消息仍只有 meaning，随后改为使用同一详情函数读取 `message.revision`，该残余 P2 已关闭。
- 原 P1 历史缺失：实现基础已补齐，包括 SQLite 完整消息快照、独立素材目录、历史版本素材白名单与失效能力凭据。HTTP 测试证明重启后仍可获取已退出清单的历史动图、封面和定义。但真实重启后的视觉验收尚未闭环，仍保留 P1 验收缺口。

剩余两项需收敛：

1. **P1 验收未闭环**：真实 Desktop 三样本双向、两个同时打开的会话切换、重启历史视觉矩阵未完整通过。CUA 拒绝访问 Codex App，随后又因 Mac 锁屏阻断浏览器补验。内置引擎 CLI 和 HTTP 测试不能冒充这些证据。
2. **入口范围待确认**：原 Spec 三个日常工具之外增加实验 `amoji_pick`，以一次 MCP 调用等待用户在配套面板点选。用户尚未回复入口问题，正式 Spec 未被改写。

另记录整体交付事实：02–19 未开始；03 Claude Code 与 04 dsh 不是“已实现、未验证”。用户对这两端工具缺失时可跳过真机验证的授权仍有效，但不会把空壳计为实现。

Standards：0 项未关闭，4 项 P2 已修复；Spec：2 项未闭环，本轴最高为 P1 验收缺口。

## 2026-09-15 补充

### Standards

本轮探针新增模式的审查发现完整顺序只按子序列判断，可能产生假阳性；另有重复的成功判定。现已严格要求相关三工具完整序列，并抽取共享成功谓词；3项聚焦测试通过，三个实际事件回放接受。两项发现复核关闭，未发现新的实际问题。

### Spec

探针顺序问题独立发现并复核关闭。整票门槛再次逐条复核：真实 Desktop 三样本双向、双任务切换、配套面板完整视觉/语义、任务卸载后冷恢复与44次实际无图像请求均有对应证据。入口边界已写入Spec，产品不依赖App内部任务API。证据和边界文件已提交 `57e831a`，因此原宿主验收及入口范围两项不再阻塞01。

结论限于所列版本和配套面板，不包含原生聊天图片目视、整个App重启或后续完整投递状态机；这些限制不阻塞01。02–19仍按各自票据交付。

本轮 Standards：0项未关闭；Spec：0项阻塞01。

## 2026-09-16：票据02独立复核

实现范围4a57952..64246ca；修补64246ca..2cb24bb。Standards初审提出停止竞态、短期凭据积累、临时绑定缓存及测试清理；主控另在安装产物复现首次冷启动竞争失败。修补后限定Standards复审逐项确认全部解决，Spec限定复核未发现固定版本、消息幂等或在途绑定回归。

最终Spec门槛复核读取共享服务接入记录及脱敏JSON，确认02五条要求满足，可关闭。真实CLI任务A探针通过；任务B完成实际收发、正确语义回应和turn.completed，但进程收尾触发截止；追加回放502失败。这些原始失败保持可见，不能声称全体端到端脚本通过或新版Desktop已经复验。新版Desktop完整产品验收仍在19。

## 2026-09-16：票据03独立复核

实现范围f7a6ac7..6bfa0d0，修补6bfa0d0..20fee0e。Spec独立审查确认五项门槛满足：普通Claude插件、真实Hook/MCP与共享面板、固定语义/精确素材、可信会话与回合、独立产物及验证边界。按授权允许以“已实现、真实Claude宿主未验证”关闭，本机版本与认证不足的补验步骤仍保留。

Standards初审发现P2反向构建防覆盖遗漏：Codex构建器可能写入Claude目录。已补写入前检查，真实产物测试先复现覆盖再验证拒绝且目录内容不变；正常Codex产物检查同样通过，限定复审确认解决且无新增破坏。未重复33项既有检查或宿主模型矩阵。

Standards另有1项P3保留到最终整支审查：`tests/shared-claude-ticket.test.ts` 在连接失败时，全局测试环境恢复及部分初始化资源清理不完整。此项已进入ledger，不作为产品功能或宿主真机通过依据，也未被悄悄丢弃。当前Spec无阻塞项；Standards无未修复P2及以上问题，仍有这1项测试清理minor待处理。

## 2026-09-16：票据04独立复核

实现82e7336..0795101。Spec发现dsh Host遗漏amoji_resolve；Standards发现Slot注册未释放、跨会话异步状态串扰、幂等等待者取消生命周期共用，以及默认测试隐式依赖下载。第一轮57a53c0补齐三工具与严格文字解析，并修复四项生命周期/测试问题；10项dsh及1项无网络检查通过。

第一轮限定复核确认原发现全部解决，但Standards在fix中发现共享submit job未绑定adapter/连接退出。第二轮8cb1ce7只修这一回归，真实连接关闭和effect卸载共4项限定检查通过；复核确认ADDRESSED且无新增破坏。Spec门槛保持通过。

票据04以“实际实现及可执行检查完成、完整dsh宿主未验证”关闭。固定源码身份、真实defineTool/SlotCore与JSDOM不替代原生Session/Connection、浏览器解码或供应商请求证据。代码未启动真实dsh或模型请求。

Standards新增1项P3留最终整支审查：scripts/build-dsh-plugin.mjs的BUILD元数据重复硬编码API、数据库、capability和dsh版本，需与真实常量保持一致。连同03的测试清理minor，均在ledger中保留。

# Codex 票据 01 实测证据

日期：2026-09-05。状态：部分通过，不能关闭票据 01。运行版本：独立 CLI `0.151.0`；Desktop 内置引擎 `0.140.0-alpha.2`；Node `24.19.0`；macOS arm64。

## 已取得的证据

| 检查 | 方式 | 结果 |
|---|---|---|
| 真实宿主身份 | 两个 Codex 二进制分别调用 MCP 探针 | 请求 `_meta.x-codex-turn-metadata` 含真实 `thread_id`、`turn_id`；与宿主事件吻合 |
| 日常工具 | CLI 0.151.0 → 本地 MCP → 实际模型 | `amoji_search` → `amoji_emit` 成功，返回精确语义和图片 Markdown |
| 人类输入 | Desktop 内置二进制以 CLI 模式运行；实际浏览器点选 | `amoji_pick` 等待后收到“挠头苦笑”的精确版本与固定语义 |
| AI 回应视觉 | 上述同会话继续搜索“加油”并发送；浏览器 AX 和截图检查 | 面板存在“你 → AI”的苦笑图与“AI → 你”的鼓励图，语义相符 |
| 初始与工具续轮输入 | 本地内存转发真实模型请求，仅记录结构统计 | 日常工具轮 4 次、双向轮 4 次，全部 `inputImageParts=0`、`imageDataUrls=0` |
| 恢复历史后继续对话 | `codex exec resume` 同一双向测试会话 | 1 次真实请求无图像输入；模型正确复述“温和自嘲…无奈” |
| 样本动画 | sharp 真实解码 + 浏览器目视 | 两帧 WebP、1000 ms、无限循环；另有 PNG 封面 |
| 包装 | 官方 plugin-creator 脚本及校验器 | manifest 通过，个人市场安装完成；安装后工具加载结果单独记录 |

被测双向会话 ID：`01a07222-5e73-7190-a954-486fab3df4d5`。人类消息 ID：`e02997a5-84ac-4f99-9545-e7c911ec1c05`；AI 消息 ID：`11440dba-98df-47f8-a7fd-6d32d97d6770`。

浏览器操作证据：选择“挠头苦笑”后出现完整含义与不适用语境；点击“发送到 Codex”后输入固定版本 `30000000-0000-4000-8000-000000000002`。AI 输出版本 `30000000-0000-4000-8000-000000000003`。浏览器可见两个方向各自正确图片。CLI 完成后面板显示连接断开并禁用发送，没有伪装成持续在线。

## 请求观测的范围

`scripts/probes/wire-relay.mjs` 只绑定回环地址，将请求原样转发至固定 OpenAI/ChatGPT 后端。认证头只在内存中透传，不写文件；不记录原始 prompt、图片字节或完整请求。统计对象来自真实 `body.input`，包括内容块类型和图像 Data URL 数量。

这证明列出的测试请求没有图像输入，**不是对所有宿主、所有会话状态或所有模型的泛化承诺**。模型理解依赖返回的文字语义，正常工具路径没有图像识别调用。浏览器截图和素材生成在开发会话中进行，未作为被测模型的输入。

转发器只实现 `/responses`，测试过程出现 `/models` 404 警告；测试使用宿主已有默认模型。另观察到已有插件缓存锁与 Notion 认证警告，没有更改这些无关组件。工具成功以实际工具返回判定，不以 CLI 退出码或模型口头声称判定。

## 已运行的失败与限制

- 第一次只读探针没有 MCP 只读注解，调用被要求审批而失败。补上符合实际行为的 `readOnlyHint` 后两个版本均成功。没有把首次失败改记为跳过。
- 安装产物首次验证使用了错误的 CLI 配置键引号，插件名被解析为包含字面引号的名称，工具未加载；正在以正确 TOML 对象配置复验。该次进程退出码为 0，但工具并未成功。
- 新 CLI 的内联 Hooks 产生有效事件；旧内置引擎本次没有对应 Hook 记录。因此实际适配直接依赖已实测的 MCP 元数据。
- 共享 CLI daemon 的控制 socket 不存在；`queue` 命令的存在不能证明可向当前 Desktop 会话输入。
- CUA 明确拒绝访问 `com.openai.codex`。导航到测试会话成功，仅说明页面导航，不算主界面目视验收。
- 两会话隔离目前有真实 HTTP/MCP 合同测试，尚未完成两个同时打开的真实 Desktop 会话切换。

## 复验

1. `npm ci && npm run build`；运行 `node scripts/probes/live-tools.mjs`，检查实际 MCP 返回。
2. 运行 `AMOJI_PROBE_PICK=1 node scripts/probes/live-tools.mjs`，在打开的面板选择并发送一个样本，观察 AI 回应图片；换样本重复。
3. 用输出的测试会话 ID 运行 `AMOJI_RESUME_THREAD=<id> node scripts/probes/live-tools.mjs`，检查回放的请求统计。
4. 在已经加载 Amoji 插件的新 Desktop 会话中调用技能，完成同样视觉检查；同时打开第二个会话，核对面板显示的完整会话标识，验证不会串话。
5. 每次仅检查本次生成的临时日志；不要提交未经筛选的登录配置、请求头、会话原始记录或面板能力 URL。

## 自动化与审查

定向测试已覆盖纯文本投影、真实图像与摘要、精确版本、会话/回合身份、凭据隔离与重试、MCP 参数拒绝、浏览器 API 来源与会话隔离。完整测试、审查与修复记录将在本次实现收尾补充。

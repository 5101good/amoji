# 票据 02：共享服务实现与 Codex 接入记录

更新：2026-09-16；环境：macOS arm64、Node.js 24.19.0。基线 `4a57952`，实现及修补为 `64246ca`、`2cb24bb`。前置票据 01 的肯定 Desktop 证据为 `57e831a`。本记录分列本地检查、新版CLI实际收发与未通过的完整探针，不把CLI证据称为Desktop新版实测；独立最终复核确认02五条门槛满足，票据关闭。

## 实际实现

- `LibraryStore` 集中保存不可变版本、当前库引用、内容寻址素材、消息快照、选择凭据和发送去重；服务独占 SQLite 写入锁。
- 独立服务通过私有发现文件、loopback 鉴权健康/API 握手和真实连接租约管理多个客户端；一个适配器退出只清理自己的关联。
- 公共 `SharedClient` 接受可信适配层的 host、instance、session、turn，服务发送只接收绑定句柄及精确引用/选择凭据。Codex MCP 与面板均通过它访问同一库。
- 旧小样数据库和素材只读复制到新库，原历史图片路径保留；未来数据库版本拒绝启动且不降级。
- 插件产物包含共享服务、公共客户端、Schema、面板、种子素材和依赖；修正原打包脚本把 npm 相对符号链接改写成开发工作树绝对目标的问题。

## 初版执行证据（64246ca）

| 命令 | 实际结果 |
|---|---|
| `npm run typecheck` | 退出 0 |
| `npm test` | 24 项通过，0 失败/跳过；包含一次实际编译与临时独立插件构建 |
| `node --test scripts/probes/live-tools-policy.test.mjs` | 3 项通过，0 失败 |
| `git diff --check` | 退出 0 |

公开边界红绿记录：先新增独立进程客户端测试，缺少共享客户端时 3 项失败；实现后 3 项通过。真实 MCP 测试先复现另一进程无法使用选择凭据，接线后通过。断连测试先复现关闭连接后 pick 仍等待，连接信号接入面板后通过。独立产物测试先检测到 5 个指回开发工作树的链接，修正打包后通过。服务终止测试先发现活跃连接清理时重新设置空闲定时器，修正后验证进程退出。旧数据测试发现开发源入口的默认种子相对路径错误，修正后迁移通过。

## 覆盖边界

- 两个真实独立客户端并发冷启动，只看到一个服务 ID 与数据根；不同会话消息隔离，一端退出后另一端继续读写。
- 服务重启后完整精确版本、动图/封面、消息 ID、展示回执和已消费选择凭据去重保留。
- API 不兼容、错误发现身份、停止身份错误、仍有活跃连接时停止均被拒绝；高版本数据库原文件字节不变。
- 两个真实 stdio MCP 进程共享选项与历史，模型工具返回仅一个文本内容块；面板按同消息 ID 回执，跨会话回执拒绝。
- 一个面板关联失效收口自己的 pending pick，另一连接仍完成实际 HTTP 点选并保存固定消息；服务终止时所有所属连接退出。
- 旧数据库、清单和旧图片文件内容不变，原会话历史及人工去重恢复；初始化后不重新合并旧原型状态。
- 临时插件产物在独立 cwd、没有 `AMOJI_SAMPLE_ROOT` 或开发 `NODE_PATH` 的环境启动，实际读取面板 HTML、动图和封面，依赖链接没有逃逸到构建工作树。

上述本地测试使用自己创建的临时数据根，没有调用真实模型、修改用户 Codex 配置、替换已安装插件或停止其他用户进程。素材继续使用票据 01 的三样本；没有预建 03–19 模块或管理功能。

## 验收边界

本票新增宿主检查使用已安装插件的Codex CLI 0.153.4与配套面板；01已通过Desktop接入，最终19再汇总Desktop完整功能基准。现有 `delivery=pending` 是保守记录，面板只确认 `rendered/fallback`，不声称宿主已 acknowledged。Linux/Windows 未做真机运行；Claude Code/dsh 仅有公共客户端接缝，适配实现属于后续票据。

安装、错误语义与复用示例见 [共享服务公共合同](../specs/shared-service-v0.1.md)。

## Standards fix round 1

审查后修复四项生命周期问题，并纳入主控在已安装 `64246ca` 产物中复现的双客户端冷启动超时：

- 停止接缝等原服务进程实际退出，确保内核写入锁释放；不再把发现文件消失当作完成。测试仅在自有子进程中延长真实 SQLite 关闭窗口，先复现错误返回，再验证立即重连成功。
- 首次候选以 75 退出时，在原 8 秒总预算内短暂随机退避并重新竞选。公开进程测试先观察真实 75 退出，再释放临时占用的真实锁；旧实现仍超时，修补后正常启动。
- 启动、检索及每分钟清理过期未消费凭据；已消费凭据随消息保留。独立服务测试推进受控时钟，核验删除效果和重启后的原 message_id 幂等。
- 增加 `unbind`，Codex 适配器在每次操作的 finally 释放临时绑定；删除每回合客户端缓存。测试用实际服务验证已结束操作的句柄不可再用，同时原在途句柄与重试仍有效。
- 测试清理仅忽略确认的 ENOENT，其余停止异常使测试失败；等待全部自有进程退出后才删除临时数据根。

客户端必需 `unbind`，服务 API 因此升为 **2**，数据库保持 **1**。新版客户端不会误连仍运行的旧 API 1 服务；管理员停止接缝继续兼容已知 API 1/2。安装产物的 serviceApi 从实际代码常量生成。

针对性执行：`npm run typecheck` 退出 0；`npm run test:file -- tests/shared-service.test.ts tests/shared-panel.test.ts tests/shared-mcp.test.ts tests/shared-migration.test.ts tests/plugin-artifact.test.ts` 共 **13/13** 通过，其中包含实际编译与独立插件构建/运行；`git diff --check` 通过。没有重跑票据 01 的真实模型矩阵或据此扩大宿主支持声明。

## 新版安装产物的实际观察

安装来源 `/Users/leilei/plugins/amoji`，插件 `amoji@personal`，版本 `0.1.0-dev.1+codex.20260915155927`，对应修补 `2cb24bb`。沿用既有构建、cachebuster和CLI安装流程；本轮没有添加或修改全局模型provider配置。所有新数据写入专用测试根，未迁移或清理用户默认库。

两个独立Codex app-server 0.153.4进程在不调用模型的情况下，都报告插件 `connected`、四个工具可用，并读到同一个服务 `bec03075-6ea2-4a5b-b04b-4adac5691e0f`，API 2 / 数据库 1。

随后两个真实CLI任务完成以下交互，均只使用可分发插件。GUI在Mac解锁后通过CUA进行，截图没有进入这些测试任务：

| 任务 | AI发给人 | 人点选并发给AI | 实际结果 |
|---|---|---|---|
| A：`01a0a5ce-3f34-7893-a522-12902e8451b4` | 庆祝 | 鼓励动图 | search/emit/pick各一次完成；模型最终回应，turn.completed，进程退出0，探针通过 |
| B：`01a0a5ce-3f34-7d73-b4f1-37ce00d2f49e` | 苦笑 | 庆祝 | 在A退出后仍能点选、发送和得到正确语义回应；turn.completed已出现，但进程收尾碰到全局5分钟截止，原探针仍记失败 |

在A退出、B仍等候点选时，服务 `e71e8da7-ef77-4e8c-ada6-1a3c953d151b` 仍健康。核心的A历史只有自己的两条消息，B当时只有自己的苦笑消息；B之后成功新增庆祝。四条消息的固定revision、素材摘要和对应message_id均保存在同一SQLite库，四条展示回执均为 `rendered`。A面板观察到5张已加载图片（含动图及历史），B点选前4张；实际截图显示对应的庆祝、苦笑与鼓励素材。

两个交互回合共观察到32次实际请求（A 11，B 21），图像内容块和图像Data URL全部为0。这包含工具等待与往返，不代表单张表情只需固定次数请求。B的追加纯文本回放出现连续502，观察到20次重试请求后被2分钟截止终止，没有最终回应；**此回放没有通过**，不改写成环境跳过或成功。

完整脱敏记录：[shared-service-codex-2026-09-16.json](evidence/shared-service-codex-2026-09-16.json)。记录保留各阶段的真实探针结果、turn.completed、文字结果、请求结构和同消息ID的库快照；不包含选择凭据、面板capability URL或认证头。

## 保留的初版失败与收尾

初次安装 `64246ca` 后，两个同时冷启动的CLI任务均未加载Amoji工具，各有3次文字请求。这不是通过记录。用该已安装产物最多计划5组独立双进程冷启动，第3组实际复现双方 `SERVICE_START_TIMEOUT`，随即停止重试；相同产物单进程启动正常。该问题与停止竞态一并由 `2cb24bb` 修复，并新增真实锁竞争的失败用例。

本轮模型探针和转发器均已退出，自有测试服务已空闲退出并经检查确认，两个临时GUI标签已关闭，专用QA任务已归档。没有后台继续调用模型。源码两轴初审及修补限定复审均已通过；上述探针收尾超时和502回放失败保持可见，不能把源码审查结论写成全部端到端检查通过。

最终门槛复核结论：五条要求均有对应证据，可以关闭02。B在业务turn.completed后的收尾超时不否定已经完成的真实收发；追加回放在Amoji工具调用前遭遇502，不构成本票共享核心缺项。保留这些失败状态，新版Desktop全功能基准仍属于19。

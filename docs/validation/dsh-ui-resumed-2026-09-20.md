# dsh 最终包原生界面补验

日期：2026-09-20。用户要求再次尝试后，保留的浏览器测试页恢复了真实点击。被测为已安装的产品提交bbcd0cc、dsh 0.1.5-rc.2、macOS arm64、Node24，隔离amoji-qa profile和数据目录。仅重启自己的QA服务，模型额度始终冻结107；本轮实际模型请求增量为0。

## 已实际通过

| 路径 | 真实观察 |
|---|---|
| 冷恢复动图 | 重启QA服务并刷新浏览器，旧AI消息的WebP图片加载成功（naturalWidth887）；点击暂停后PNG封面加载成功，再点击恢复播放。 |
| 历史版本选择 | 管理界面选择A2的旧版本A1，名称、固定含义和图片切回旧定义；不隐式更改当前默认版本。 |
| 编辑新版本与可选字段删除 | 编辑QA个人副本，清空tone/creator/source/tags，保存预览并在图片加载后勾选确认；实际生成新revision，三个可选key被省略，tags为空，旧revision保留。 |
| 必填语义门禁 | 用实际键盘删除meaning，保存草稿允许，预览返回INVALID_SCHEMA，不能确认；补齐meaning后完整预览和确认成功。 |
| 已确认编辑退出 | 点击“结束当前编辑”直接返回草稿列表，没有再次弹出阻碍操作的确认框。 |
| 建议不可用与手工继续 | 保留未保存名称和意图后，仅终止核对过身份的QA核心；点击生成建议返回中文CONNECTION_CLOSED，输入仍在。点击“重新连接共享库”后保存并预览成功，未依赖建议器完成定义。 |
| 并发版本冲突 | 使用旧版本草稿确认时返回ENTRY_CONFLICT，并显示“本次输入已保留”。点击“基于当前版本继续，保留本次填写”，重新保存/预览/勾选/确认成功，名称和输入未丢失。 |
| 真正缺图回退 | 暂时移动QA旧动图blob（自动恢复保护），刷新页面显示固定文字“[给你加油]”及BLOB_MISSING；恢复原字节后再刷新，图片重新成功加载。没有删除资产或修改不可变定义。 |
| A1历史可见 | 从真实侧边栏打开版本与暂停A，看到原图片、原名称和感谢含义；展开详情显示revision 0e63ec92-5310-4ab6-a844-932b847b8705。 |
| 会话切换 | 在A选择“好开心”但不发送，切换到B时选择器关闭；B打开选择器无选中项、发送禁用，历史仍为A2庆祝图片和含义。返回A，仍为A1。没有额外消息或模型回合。 |

原个人副本asset为c0b6a913-db1b-4209-998c-3b58afcf908c。清空可选字段确认生成revision `41b3bd95-cddc-4afb-b1f1-ccd0e517a0bf`；断连/冲突恢复确认生成`446dd8b3-7c6d-4d40-b17d-38e2dc3efb1c`。结束后恢复此QA条目的归档状态，目录回到24项基础表情。所有旧版本和调试草稿保留，日常用户库没有被这些故障注入触及。

浏览器填充API的fill空字符串没有实际更新此受控表单；核对页面后改用全选/Backspace，才执行删除字段检查。没有把“工具调用返回成功”当作输入已删除。图片缺失前历史已经rendered，服务记录不降级；本轮证明的是实际页面出现固定文字fallback，不能声称持久回执变为了fallback。

## 证据与边界

本地记录：`.local/dsh-focus/UI-resumed/live-checks.json`、`optional-fields.json`、`core-interruption.json`、`missing-blob.json`，以及本任务CUA真实AX/DOM观察。缺图文件已还原，新核心PID与故障前不同，模型请求仍107，额度未增加。

日常3080页面再次尝试仍返回ERR_BLOCKED_BY_CLIENT，未切换未授权控制渠道绕过；因此日常web profile已有安装和认证Host读取证据，但其页面未在应用内浏览器验收。这与65501隔离页已恢复操作是两个独立事实，不再要求用户关闭已经不影响QA页的旧确认框。

未知提交/管理写入“恰好丢失响应”的时序仍依赖实际Client与服务自动故障验证，本轮没有通过真实浏览器注入该精确时序；普通会话切换与真实核心断连已另行验证。强制上下文压缩、独立双Host退出以及其他OS/版本/PTC没有新增通过声明。详细分层状态见[dsh分发验收](dsh-release-acceptance.md)。

# 安全政策

[English](SECURITY.en.md)

## 报告问题

如仓库 [Security](https://github.com/5101good/amoji/security) 页面提供私密漏洞报告入口，请使用它。若入口不可用，请创建仅请求私密联系渠道的 Issue，不公开利用细节、凭据、数据库或会话内容。项目暂未承诺漏洞响应时限。

报告请提供 Amoji/dsh/Node 版本、系统与架构、影响边界、最小复现和经过脱敏的日志。请先在隔离数据目录复现，不修改他人实例。

## 当前安全边界

- 共享核心是本地回环服务，使用服务身份、本地能力凭据与连接/会话绑定。不能将其当作面向不可信网络的公共 API。
- 模型日常工具仅接收文字投影；不接受任意图片路径、目标会话或语义覆盖。库管理不是模型任意写入接口。
- AI 建议复用 dsh 模型服务和凭据，仅传用户文字意图与生成指令，不传图片或聊天历史。用户仍需审阅含义及许可。宿主/提供方的数据保留政策由其自身决定。
- `.amoji` 导入校验 ZIP 路径、CRC、Schema、摘要、实际媒体解码和资源预算；它不是执行脚本的插件格式。
- 运行目录含个人库、消息、素材以及可能带访问秘密的服务发现信息。文件权限不等于内容加密；不要将其作为公开诊断附件。

软件使用宿主网络与本机依赖。安装 tgz 时包管理器会下载依赖；Amoji 的 MIT 不替代依赖许可证，也不代表第三方组件无漏洞。安全修复以当前 dsh 维护版本为目标；legacy Codex/Claude Code 没有独立安全支持承诺。

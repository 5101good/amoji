# Amoji v0.1 票据拆分修订稿

日期：2026-09-05。状态：已按用户指定的 Codex 验证基准与 imagegen 素材方案修订；尚未发布到 Issue Tracker，也未应用 ready-for-agent。本文是拆分方案，正式发布仍按配置的追踪器一票一条记录。

范围：本期交付共享核心和三个适配实现，以 Codex 完成完整真实接入验证。dsh、Claude Code 的验证工具不可用时允许先实现、跳过真机验证并明确记账；工具可用时继续验证。实际失败不能改记跳过，空壳不能算实现。

## 阅读规则

- 保留 19 张票和 64 条用户故事覆盖，按前置依赖重排前四张：原 04 共享核心成为 02；原 02 Claude Code 成为 03；原 03 dsh 成为 04；05–19 编号保持。
- 开工前沿为 01。主线是 01 → 02 → 完整功能；03、04 基于 02 实现，不再以它们的宿主实测阻塞共享核心。
- 19 仍要求 03、04 的适配代码和可执行检查完成；只豁免因验证工具缺失而无法执行的真机部分。
- Codex 是真实交互测试基准，覆盖用户选表情、模型文本、视觉展示、历史和多会话；其他适配器执行公共合同检查，宿主工具可用时补齐同样测试。
- 图片使用 Codex imagegen 技能的内置工具生成，保存项目素材、提示和来源。动图另行封装并验证；生成静态图不等于完成动画。
- 素材生成与目视检查属于开发阶段，被测 Codex 会话不预先携带图片输入；产品首版不新增绘图入口，运行中不调用 imagegen。
- 每票完成一个可演示或通过公开合同核对的窄闭环，包含需要的数据、接口、界面和测试；不安排横向空模块建设。
- 实现状态、构建/合同检查、宿主实测分开记录。未开始检查不能伪装为工具缺失；真实失败保留失败状态。
- 固定语义、精确版本、历史保留与投递身份约束对所有适配器一致，不因某宿主未验证而降低实现合同。

## 提议票据

### 01：Codex Desktop 三样本双向接入

**前置依赖：** 无，可独立开始。

**交付行为：** 在真实 Codex Desktop 中选一个固定表情让 AI 收到语义，AI 再选表情并显示对应图片；形成可复现的插件接入小样。

**关联用户故事：** 02、03、09、10、11、12、14、16、17、21、27、53、55、56、57、58、60、63、64。

**验收草案：**

- [x] 使用 Codex imagegen 技能生成测试图片并保存到项目，记录提示与固定语义；准备两张静态图和一张另行封装、验证的短循环动图，跑通两个方向，仅用可分发插件机制。
- [x] 视觉与模型文本关联同一确定版本；在不预先携带生成图片的 Codex 测试会话中检查首次发送、续聊和重开历史的实际模型请求。
- [x] 两个同时打开的会话具备明确目标，基本切换测试不串话；观察可信会话和回合身份的获取方式。
- [x] 原生界面或配套面板均可；仅复制文本、当前开发会话专有工具或无法观察模型请求不能计作完整通过。
- [x] 输出明确版本的操作、视觉与请求证据；Codex 未达完整接入门槛时保持阻塞，不套用其他宿主的工具缺失跳过规则。

2026-09-15 关闭：安装版真实 Desktop 验证及独立 Spec 复核通过。版本、配套面板范围和44次实际请求见 [补验证据](../validation/codex-ticket-01-2026-09-15.md)，证据提交 `57e831a`。01关闭不代表02–19已完成。

### 02：Codex 接入共享本地表情库

**前置依赖：** 01「Codex Desktop 三样本双向接入」。

**交付行为：** Codex 的多个独立会话连接同一本地表情库，重启后继续使用同一版本，并提供其他适配器可复用的公共合同。

**关联用户故事：** 01、04、13、21、49、50、64。

**验收草案：**

- [x] 以 Codex 接入的肯定运行证据收敛核心合同，不等待 dsh、Claude Code 的真机验证。
- [x] 将样本接入共享版本、素材与消息服务，在独立 Codex 会话中实际看到相同表情；其他适配器通过同一公共接口访问。
- [x] 库独立于插件缓存且无需账号；多连接同时启动不会产生多份用户库或静默覆盖。
- [x] 单次发送固定版本、快照与素材引用，核心重启后仍可取用；公共合同不接受动态语义或任意目标会话。
- [x] Codex 真实收发表情并关联投递与显示消息标识，具备基础去重；提供公开合同检查供后续适配复用。

证据：[共享核心实现与真实Codex接入](../validation/shared-service-ticket-02.md)。独立最终复核确认五条门槛满足；B探针收尾超时及后续502回放失败保持记录，不写成全部脚本通过。

### 03：实现 Claude Code 适配并记录验证状态

**前置依赖：** 02「Codex 接入共享本地表情库」。

**交付行为：** 将 Claude Code 插件和配套面板接到共享核心，完成双向交互所需的实际适配；验证工具不可用时交付实现与明确的未验证记录。

**关联用户故事：** 02、03、09、10、11、12、14、16、17、21、27、54、55、56、57、58、60、63、64。

**验收草案：**

- [x] 完成可分发插件的真实工具、面板、会话绑定和展示适配逻辑，连接共享核心；不能以空壳、TODO 或固定成功返回计为实现。
- [x] 执行可用的构建、类型及公共合同检查，验证输入只传文本语义、输出关联正确版本、配套面板读取正确素材。
- [x] 会话与回合身份按有依据的宿主合同处理；预览通道能力与普通插件能力分别标注，不编造已验证的原生接口。
- [x] 验证工具可用时运行真实三样本、双会话和回放测试；不可用时记录缺失工具、跳过用例及补验步骤，已运行失败仍记失败。
- [x] 分别报告实现、可执行检查和宿主验证状态；允许“已实现、宿主未验证”交付，不因工具缺失阻塞 Codex 主线。

2026-09-16关闭：实现及合同检查通过，独立Spec门槛通过；Standards构建防覆盖问题修复并复审通过，测试部分初始化清理作为minor保留到最终审查。提交6bfa0d0、20fee0e。**真实Claude宿主未验证**：本机2.1.177未登录且低于公开prompt_id合同最低版本。详见[实现/检查/补验记录](../validation/claude-ticket-03.md)。

### 04：实现 dsh 适配并记录验证状态

**前置依赖：** 02「Codex 接入共享本地表情库」。

**交付行为：** 将 dsh Host/Client 插件接到共享核心，实现会话输入和视觉输出；验证工具不可用时保留完整适配与可复验说明。

**关联用户故事：** 02、03、09、10、11、12、14、16、17、21、27、52、55、56、57、58、60、63、64。

**验收草案：**

- [x] 按既有源码依据实现可分发的 Host/Client 适配、表情选择与真实客户端渲染逻辑，不能只提供后端 presenter 或固定成功返回。
- [x] 执行可用的独立构建、类型及公共合同检查，验证模型文本投影、精确版本和素材展示映射。
- [x] 实现可回放记录及会话/回合绑定合同；明确目标 dsh 基线，不把文档依据等同于实际运行证明。
- [x] 工具可用时执行真实三样本、双会话及回放；缺少验证工具时记录跳过范围、具体原因和复验步骤，实际失败仍记失败。
- [x] 输出可检查的插件产物与实现/验证状态；宿主未验证不阻塞 Codex，但不能宣称 dsh 已验证支持。

2026-09-16关闭：Host/Client及三个日常工具完整接入共享核心，固定基线实际工具/Slot与组件检查通过；两轮限定修补及独立复核通过。实现提交0795101、57a53c0、8cb1ce7。**真实dsh宿主未验证**，不以固定源码或JSDOM事件冒充完整宿主；具体构建、验证边界及补验步骤见[04验证记录](../validation/dsh-ticket-04.md)。BUILD元数据硬编码minor保留最终审查。

### 05：搜索表情并让 AI 按语境选择

**前置依赖：** 02「Codex 接入共享本地表情库」。

**交付行为：** 用户按文字搜索并发送，AI 通过少量完整语义候选选择合适表情；没有合适内容时正常用文字回应。

**关联用户故事：** 07、08、09、18、19、20、21、64。

**验收草案：**

- [x] 名称、标签和语义参与本地检索，用户能预览语义并选择结果。
- [ ] AI 获得精确版本与绑定的选择凭据，发送后展示匹配视觉；不存在的引用明确失败。
- [ ] 查询返回候选预算受控，不注入整库或裁剪重要语义字段；记录文本及工具开销。
- [ ] 以真实语境验证自主选择与无合适候选的文字回应，不断言唯一正确 ID。

2026-09-16实现及本地合同完成：6528461、2b62375；共享检索、完整8KiB预算、精确凭据和三端用户搜索已实现，两轴复审通过。**整票保留待验**：真实Codex语境选择、匹配视觉与实际模型/工具成本统一在19回填，不能以合同检查代替。见[05记录](../validation/search-ticket-05.md)。

### 06：动图播放、暂停与替代呈现

**前置依赖：** 02「Codex 接入共享本地表情库」。

**交付行为：** 用户在三端看到正确的短循环动图，能够暂停并使用静态封面；缺图时看到准确替代文本。

**关联用户故事：** 27、28、29。

**验收草案：**

- [ ] 以公共素材合同实现三端视觉能力；在 Codex 实测动图，并校验时长、帧数、尺寸与静态封面，不依赖样本硬编码；其他宿主真机部分可因工具缺失跳过。
- [ ] 提供动画暂停与减少动态效果行为；恢复会话仍对应相同版本的主图与封面。
- [ ] 加载失败显示该版本的替代文本和异常，不识图、不替换其他图片。
- [ ] 静态与动图的语义不随播放状态变化，实际模型输入仍为文本。

2026-09-16实现及本地合同完成：061dcb3、335afe4；统一真实媒体解码、播放/暂停与减少动态效果、精确历史及失败回退已实现，两轴及限定复核通过。**整票保留待验**：安装版Codex的真实播放、暂停、恢复与实际模型请求统一在19回填。见[06记录](../validation/media-ticket-06.md)。

### 07：手工创建表情并在会话中发送

**前置依赖：** 06「动图播放、暂停与替代呈现」。

**交付行为：** 用户上传静态图或动图、填写语义、预览确认后进入共享库，并在 Codex 实际发送；其他适配器复用相同合同。

**关联用户故事：** 30、32、33、34、35、36。

**验收草案：**

- [ ] 原始素材进入草稿，用户可编辑名称、含义、语气、语境和替代文本。
- [ ] 确认前草稿不可被 AI 检索或发送；确认后分配确定版本并进入共享库。
- [ ] 沿用已完成的素材校验和动图封面能力，非法或超预算内容给出明确错误。
- [ ] 不依赖任何模型即可走完创建与发送；重启后新表情仍可用。

### 08：用文字意图辅助定义表情语义

**前置依赖：** 07「手工创建表情并在会话中发送」。

**交付行为：** 用户描述想表达什么，系统提出语义建议，用户修改确认后发送；建议不可用时仍可手工完成。

**关联用户故事：** 31、32、33、34、35。

**验收草案：**

- [ ] 建议器只接收文字意图和用户提供的说明，不必访问视觉素材或调用识图模型。
- [ ] 建议只进入草稿，不自动确认或修改已用版本。
- [ ] 用户接受、修改或放弃建议后均能继续手工创作。
- [ ] 建议超时或不可用时保持已填内容，并通过真实会话发送最终确认版本。

### 09：编辑自己的表情并保持旧消息不变

**前置依赖：** 07「手工创建表情并在会话中发送」。

**交付行为：** 用户修改自建表情后发送新版，原会话与重开的历史仍显示旧版图文。

**关联用户故事：** 13、44、46、58。

**验收草案：**

- [ ] 保存编辑生成新版本；未修改原版本或发送接口语义。
- [ ] 新版在共享库可用，Codex 实际发送后旧消息快照与素材引用不变；其他适配器保持同一版本合同。
- [ ] 选择后发生编辑时不静默切换待发表情；明确使用已选版本或报告选择失效。
- [ ] 覆盖含义改变和视觉替换两类真实编辑，重开会话验证旧版。

### 10：导入完整表情包并立即使用

**前置依赖：** 06「动图播放、暂停与替代呈现」。

**交付行为：** 用户导入带语义和素材的完整 Amoji 包，校验成功后在 Codex 立即发送，其他适配器复用同一共享库。

**关联用户故事：** 37、38、39、40、61。

**验收草案：**

- [ ] 合法包无需逐张重做语义确认，静态与动图均可用。
- [ ] 校验协议、默认引用、素材闭包、实际摘要与媒体约束，拒绝路径越界及包资源超限。
- [ ] 重复相同版本幂等；同 ID 不同内容拒绝覆盖，非法包不留下半导入的可用记录。
- [ ] 导入不伪造本机作者归属、不自动切换已有个人版本，也不公开任何内容。

### 11：导出表情包并在新环境恢复

**前置依赖：** 10「导入完整表情包并立即使用」。

**交付行为：** 用户导出选定表情，在新的本地数据环境中导入后获得相同视觉与语义并能发送。

**关联用户故事：** 41、42、43、61。

**验收草案：**

- [ ] 导出完整确定版本和必要素材，重复素材去重。
- [ ] 包不包含会话、设置、使用记录、凭据或原机器绝对路径。
- [ ] 新环境导入后静态及动图摘要、语义和默认引用一致，真实会话可发送。
- [ ] 导出当前版本不清理原库历史所需素材；失败时不产生看似完整的损坏包。

### 12：将内置或导入表情改成个人副本

**前置依赖：** 09「编辑自己的表情并保持旧消息不变」；10「导入完整表情包并立即使用」。

**交付行为：** 用户修改内置或导入表情形成自己的版本，原作者定义及旧消息保持不变。

**关联用户故事：** 45、46。

**验收草案：**

- [ ] 两个来源都通过真实编辑操作生成新的表情身份并保留来源关系。
- [ ] 后续编辑只更新个人副本的版本，其他原版本引用不变。
- [ ] 导入同一上游包的后续版本不覆盖个人副本或悄悄改变默认选择。
- [ ] 在 Codex 分别发送原版与个人副本验证图文独立，其他适配器执行公共合同检查，缺少工具的真机部分单列跳过。

### 13：归档表情并保留历史展示

**前置依赖：** 02「Codex 接入共享本地表情库」。

**交付行为：** 用户将表情从可用库归档后，新发送不再选到它，历史仍可正常显示。

**关联用户故事：** 16、47、58。

**验收草案：**

- [ ] 归档从用户新发列表和 AI 新发候选中移除，已有历史精确解析仍可用。
- [ ] 消息引用的素材保留，重启与恢复历史不丢图。
- [ ] 归档前产生但尚未使用的选择凭据不能悄悄绕过当前可用性规则。
- [ ] 缺失素材显示原替代文本与错误，不取同名或最新版本补图。

### 14：控制 AI 表情风格、频率与暂停

**前置依赖：** 05「搜索表情并让 AI 按语境选择」。

**交付行为：** 用户修改共享个人偏好后，Codex 的后续表达实际遵循风格、频率和暂停；其他适配器读取并执行同一策略合同。

**关联用户故事：** 22、23、24、25、50、60。

**验收草案：**

- [ ] 设置持久保存，在多个 Codex 会话中实测更新可见，默认克制并限制重复；其他适配器按公共合同读取，真机验证可因工具缺失跳过。
- [ ] 核心在发送时执行暂停及数量规则，依赖可信回合标识而非模型自报值。
- [ ] 暂停前已检索的凭据不能绕过新策略，暂停后用户手动发送与语义理解仍可用。
- [ ] 用允许多个合理表情的真实场景检查风格选择；表情不成为其他操作授权。

### 15：断连、超时和重连后正确处理发送

**前置依赖：** 02「Codex 接入共享本地表情库」。

**交付行为：** 用户遇到故障时看到准确的投递与展示状态，重试和重连不会重复发送或发到其他会话。

**关联用户故事：** 14、15、26、55、57、64。

**验收草案：**

- [ ] 覆盖提交前后断连、宿主超时、服务重启与显示失败，分别呈现失败、未知和仅文本回退。
- [ ] 相同请求或选择凭据重试返回原消息；查询不到宿主结果时不盲目重发。
- [ ] 绑定丢失或切换时保持原目标或停止发送，不选最近会话代替。
- [ ] 两个会话交错运行、暂停、恢复和重连时验证不串话，并保留可追踪证据。

### 16：多客户端并发编辑不丢失修改

**前置依赖：** 09「编辑自己的表情并保持旧消息不变」；14「控制 AI 表情风格、频率与暂停」。

**交付行为：** 两个客户端同时修改同一表情或设置时，用户能看到冲突并保留自己的编辑内容。

**关联用户故事：** 48、50。

**验收草案：**

- [ ] 根据读取时的修改序号拒绝过期覆盖，返回当前可见状态。
- [ ] 冲突界面保留本次未保存内容，让用户刷新或重新编辑，不自动覆盖另一端。
- [ ] 分别验证表情修订与个人设置，并在解决冲突后通过实际收发表情确认结果。
- [ ] 不创建两份互相分叉的默认用户库，历史版本保持不变。

### 17：升级或移除单端插件后继续使用共享库

**前置依赖：** 02「Codex 接入共享本地表情库」。

**交付行为：** 在 Codex 验证插件升级、重装或移除后共享库与历史保留；其他适配器实现相同生命周期合同并分别报告验证状态。

**关联用户故事：** 49、51、59。

**验收草案：**

- [ ] 用实际 Codex 插件包执行升级、移除与重装，验证资产及消息保留；其他宿主执行可用打包/合同检查，工具缺失时记录真机跳过。
- [ ] 多客户端连接时正确管理共享服务生命周期，不因单端离线终止其他会话能力。
- [ ] 协议不兼容时明确提示，不破坏数据库或静默降级。
- [ ] 提供可复现恢复步骤，验证独立 Codex 会话继续完成双向表达，不以合同桩结果声称其他宿主已运行通过。

### 18：交付可直接使用的基础表情库

**前置依赖：** 10「导入完整表情包并立即使用」。

**交付行为：** 新用户加载基础表情库后即可在真实会话中使用约 20–30 个完整表情。

**关联用户故事：** 05、06、27、29、61。

**验收草案：**

- [ ] 使用 Codex imagegen 技能制作已定六组表达方向的图片，保存项目素材、提示和来源；包含静态与另行封装验证的动图，并共同评审视觉与语义。
- [ ] 所有内容采用普通表情包格式，具备素材、封面和替代文本，无占位摘要。
- [ ] 确认素材可随项目分发，记录许可与来源；内置库不会读取或公开个人内容。
- [ ] 在 Codex 实际挑选基础内容收发，其他适配器使用同一普通表情包并执行可用检查；不以清单数量或 Codex 结果代替其他宿主验证。

### 19：交付三端实现并通过 Codex 完整验收

**前置依赖：** 03「实现 Claude Code 适配并记录验证状态」；04「实现 dsh 适配并记录验证状态」；08「用文字意图辅助定义表情语义」；11「导出表情包并在新环境恢复」；12「将内置或导入表情改成个人副本」；13「归档表情并保留历史展示」；15「断连、超时和重连后正确处理发送」；16「多客户端并发编辑不丢失修改」；17「升级或移除单端插件后继续使用共享库」；18「交付可直接使用的基础表情库」。

**交付行为：** 交付共享核心和三个实际适配器，以 Codex 完成干净安装及完整用户旅程；其他宿主工具缺失的真机测试明确记录为未验证。

**关联用户故事：** 01、02、03、04、05、49、52、53、54、59、61、62、64。

**验收草案：**

- [ ] 固定依赖与兼容范围，Codex 干净安装后完成全部基准行为；其他两端实际适配和可执行检查完成，缺失工具的宿主用例单列跳过。
- [ ] 汇总 Spec 的 20 个验收场景与本票据前置证据；跨客户端行为用独立 Codex 会话/连接及公共合同验证，不据此宣称其他两端真机通过。
- [ ] 在 Codex 运行受控语境场景，记录文本 Token、工具往返、实际图像输入与本地耗时；基准完整接入不能套用工具缺失的跳过规则。
- [ ] 准备协议、许可证、素材来源与许可、核心和三个插件的开源分发候选物；记录 imagegen 素材生产信息，不凭空创建外部发布项目。
- [ ] 清楚分列实现状态、构建/合同检查、宿主实测、缺失工具和补验步骤；实际失败与环境跳过不同，个人表情库保持默认私有。

## 依赖理由

- 02 只依赖 01 的 Codex 肯定接入证据，移除其他宿主真机验证的前置阻塞。
- 03、04 需要 02 的实际共享核心和公共接口才能交付可连接的适配实现，二者互不依赖。
- 05、06、13、15、17 只依赖 02，彼此不制造排队关系。
- 07 与 10 复用 06 的通用媒体合同，分别完成创作和导入。
- 08 依赖 07 的手工创作，09 依赖 07 提供的自建表情。
- 11 依赖 10 完成迁移回环，12 依赖 09、10 验证实际来源的个人副本。
- 14 依赖 05 的 AI 语境选择，16 依赖 09、14 的真实编辑入口。
- 18 依赖 10 的普通包导入，不依赖用户编辑器或建议器；素材由 imagegen 制作。
- 19 依赖各功能分支末端及 03、04 的实现完成，不要求工具不可用的其他宿主真机测试通过。

## 审阅与发布状态

用户已明确 Codex 基准、其他宿主有条件跳过验证及 imagegen 素材方案。本稿据此调整实现顺序和验收合同，不再把三端实测全部通过作为本期开工或交付条件。

Issue Tracker 仍未配置。按 to-tickets 工作流需运行 /setup-matt-pocock-skills；本地追踪器按一票一文件发布，远端追踪器按一票一 Issue 发布并设置阻塞关系。发布使用 ready-for-agent，不修改或关闭父 Issue。

当前只更新规格与票据方案，未创建正式票据、未开始插件实现、未生成图片或执行客户端验证。

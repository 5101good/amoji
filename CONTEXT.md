# Amoji 领域词汇

Amoji 是供人与 AI 共同使用的独立表情体系。人通过视觉感受表达，AI 通过随表情定义的文本语义理解表达。

## 语言

**表情（Expression）**：
一个可重复使用的表达单元，由视觉内容和文本语义共同构成。
_避免_：把表情称为情绪识别结果、模型工具或单独一张图片。

**视觉素材（Visual）**：
表情供人观看的静态图片或短循环动图。
_避免_：把视觉素材等同于已具备语义的完整表情。

**表情语义（Semantics）**：
系统与用户共同定义并确认的表达含义，可包含语气、适用语境和替代文本。
_避免_：本次发送意图、隐藏指令、用户真实心理状态。

**表情版本（Revision）**：
某个表情在一个确定时点的完整定义，包含当时的视觉内容与语义。已使用的版本不会因为后续编辑而改变。
_避免_：用“最新表情”指代历史消息中的表情。

**表情消息（Expression Message）**：
用户或 AI 在一次交流中使用某个确定表情版本所形成的消息。
_避免_：临时修改语义的表情、重新定义表情。

**个人副本（Personal Copy）**：
用户基于内置或导入表情编辑形成、由自己维护的表情。它保留来源关系，但不改变原表情。
_避免_：全局覆盖、原作者更新。

**表情库（Library）**：
用户在本机管理和使用的表情集合，以及自己选择使用的版本。
_避免_：公共市场、模型上下文。

**表情包（Pack）**：
可供分享、导入或导出的表情集合，携带完整版本定义及其视觉素材。
_避免_：只有图片的素材压缩包、插件安装包。

**语义建议（Semantic Suggestion）**：
系统根据用户提供的文字意图形成、供用户编辑和确认的候选语义。
_避免_：图片识别的必然结果、无需确认的最终定义。

**配套面板（Companion Panel）**：
与宿主会话配合使用的视觉界面，提供表情选择、展示与管理。
_避免_：把独立打开的图库直接称为已接入会话。

**宿主适配器（Host Adapter）**：
连接 Amoji 与 dsh、Codex 或 Claude Code 的接入部分，使表情能够参与该宿主的真实会话。
_避免_：三个互不相通的表情体系。

